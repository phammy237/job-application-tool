{
  "name": "resume-compiler",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone LaTeX->PDF compile service for Career OS Resume Studio. Deployed separately from the main Vercel app (see README.md) — not part of the root npm workspaces.",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^20.14.0"
  }
}
