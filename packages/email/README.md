# packages/email

Server-only Gmail integration: OAuth token exchange, message search, deterministic
classification rules, Claude fallback for ambiguous cases, and minimal-retention signal
storage. Never imported by client-side code.

**Status:** not yet scaffolded. Build begins in Phase 5. See `docs/EMAIL_INTEGRATION.md`.
